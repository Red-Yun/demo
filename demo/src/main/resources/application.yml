spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/first?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: 123456
    type: com.alibaba.druid.pool.DruidDataSource

server:
  port: 8083

#mybatis:
#  #指定 MyBatis 主配置文件的位置
##  config-location: classpath:config/mybatis-config.xml
#  #指定 mapper 文件的位置
#  mapper-locations: classpath:mapper/*.xml
#  # 对应实体类的包名
#  type-aliases-package: com.springboot.entity